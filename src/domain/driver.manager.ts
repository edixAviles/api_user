import Driver from "../model/driver"
import DriverInput from "./driver.input"

class DriverManager {
    insert(driverInput: DriverInput): Driver {
        // Logica para agregar el driver
        return new Driver()
    }
}

export default DriverManager
